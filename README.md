# ShopCore

Ядро интернет-магазина на Python с использованием ООП.

## Описание проекта

Проект разработан в рамках курса "Объектно-ориентированное программирование" и представляет собой основу для интернет-магазина.

### Домашнее задание 14.1 (Введение в ООП)
- Класс `Product` с атрибутами: name, description, price, quantity
- Класс `Category` с атрибутами: name, description, products
- Атрибуты класса `Category`: `category_count` и `product_count` для автоматического подсчета
- Тесты для проверки инициализации объектов и подсчета категорий/товаров

### Домашнее задание 14.2 (Режимы доступа)
- Атрибут `products` класса `Category` сделан приватным (`__products`)
- Добавлен метод `add_product()` для безопасного добавления товаров
- Реализован геттер `@property` для атрибута `products` (возвращает строку)
- Атрибут `price` класса `Product` сделан приватным (`__price`)
- Реализованы геттер и сеттер для атрибута `price` с валидацией (защита от отрицательной цены)
- Добавлен класс-метод `new_product()` для создания продукта из словаря

### Домашнее задание 15.1 (Магические методы)
- Реализован `__str__` для класса `Product` (красивое строковое представление)
- Реализован `__str__` для класса `Category` (с подсчётом общего количества товаров на складе)
- Реализован `__add__` для класса `Product` (сумма стоимостей товаров на складе: price × quantity)
- Оптимизирован геттер `products` в `Category` (теперь использует `__str__` продукта)
- Написаны тесты для новой функциональности
- Покрытие тестами > 75%

### Домашнее задание 16.1

#### Реализованный функционал:

**1. Классы-наследники**
- Создан класс `Smartphone`, наследующий от `Product`, с дополнительными атрибутами:
  - `efficiency` - производительность
  - `model` - модель
  - `memory` - объем встроенной памяти
  - `color` - цвет

- Создан класс `LawnGrass`, наследующий от `Product`, с дополнительными атрибутами:
  - `country` - страна-производитель
  - `germination_period` - срок прорастания
  - `color` - цвет

**2. Ограничения сложения**
- Доработан метод `__add__` таким образом, что складывать можно только товары одинаковых классов
- При попытке сложить объекты разных классов (например, `Smartphone` и `LawnGrass`) выбрасывается `TypeError`
- Для реализации используется функция `type()`

**3. Ограничения добавления продукта в категорию**
- Доработан метод `add_product` с проверкой типа добавляемого объекта
- Можно добавлять только объекты класса `Product` или его наследников
- Для реализации используется функция `isinstance()`

**4. Тестирование**
- Написаны тесты для классов `Smartphone` и `LawnGrass`
- Написаны тесты для проверки ограничений сложения
- Написаны тесты для проверки ограничений добавления продукта
- Покрытие кода тестами превышает 75%

### Домашнее задание 16.2
- Создан абстрактный базовый класс `BaseProduct` с абстрактным методом `get_product_info`
- Реализован класс-миксин `PrintReprMixin`, который выводит в консоль информацию о создании объекта
- Миксин добавлен в цепочку наследования класса `Product`
- Классы `Smartphone` и `LawnGrass` наследуются от `Product`
- Написаны тесты для новой функциональности, покрытие кода тестами составляет 93%
- Выполнено дополнительное задание: создан класс `Order` с расчетом итоговой стоимости
- Для классов `Order` и `Category` выделен общий абстрактный базовый класс `BaseEntity`

### Домашнее задание 17.1 - Исключения

   - Реализована проверка на нулевое количество при создании продукта (выбрасывается `ValueError`).
   - Добавлен метод `middle_price` в класс `Category` для расчета средней цены с обработкой пустой категории.
   - Написаны тесты для новой функциональности, покрытие кода тестами составляет 94%.
   - Выполнено дополнительное задание: создано пользовательское исключение `ZeroQuantityError`, доработан метод `add_product` с использованием блоков `try/except/else/finally`.

### Тестирование
- Написаны тесты для функциональности
- Покрытие тестами > 75%
- Отчет о покрытии добавлен в репозиторий

### Качество кода
- Код проверен линтером Flake8 (0 ошибок)
- README.md обновлен с описанием функционала 14.1 и 14.2

## Установка

```bash
poetry install

```

## Запуск
```bash
poetry run python main.py
```
## Тестирование
```bash
poetry run pytest tests/ --cov=src --cov-report=html
```
## Структура проекта
```bash
shopcore/
├── src/              # Исходный код
├── tests/            # Тесты
├── main.py           # Пример использования
└── README.md         # Документация
```
## Лицензия
MIT